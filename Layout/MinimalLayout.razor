@using Microsoft.AspNetCore.Components.Web
@inherits LayoutComponentBase

<div class="minimal-layout">
    @Body
</div>

@code {
    // No additional code needed - this layout just renders the page content
}
